<div>
  <p><%= comment.body %></p>

  <% if policy(comment).update? %>
    <small>
      <%= link_to 'Edit', edit_post_comment_path(@presenter.post, comment) %> |
      <%= link_to 'Delete', [@presenter.post, comment], method: :delete, data: { confirm: 'Are you sure?' }, class: 'pull-right' %>
    </small>
  <% end %>
</div>
