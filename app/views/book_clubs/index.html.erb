<% if current_user.admin? %>
  <%= render partial: "admin_index", locals: { current: @current, upcoming: @upcoming, past: @past } %>
<% else %>
  <%= render partial: "member_index", locals: { current: @current, upcoming: @upcoming, past: @past } %>
<% end %>
